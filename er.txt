first the system generates a pop-up window asking "New User" or "Existing User" if user choose new user system will ask the user's name and take permission to capture 500 images and store it into a file of user's name then it will classify the pictures into anger, sad, happy, pain, neutral, surprise, fear and any other facial expression then the model will be trained with those pictures as a dataset after that it will generate a new pop window for detecting the facial expression of user continuously till user terminates it. if user chose existing user then system ask the name of the user and matches with existing data if yes then it will directly generate the pop up window for detecting emotions otherwise generates the window to create an account.  generate me an python code for it